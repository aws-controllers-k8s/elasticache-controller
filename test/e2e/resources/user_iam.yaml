apiVersion: elasticache.services.k8s.aws/v1alpha1
kind: User
metadata:
  name: $USER_ID
spec:
  accessString: $ACCESS_STRING
  authenticationMode:
    type: iam
  engine: valkey
  userID: $USER_ID
  userName: $USER_ID
